# Construir el prompt de sistema para modo JSON (together / openrouter)

Antepone la \`persona\` al bloque de instrucciones de esquema
compartido, de modo que una persona personalizada nunca elimine la lista
de campos.

## Usage

``` r
.acep_provider_json_system_prompt(persona, campos_texto)
```
